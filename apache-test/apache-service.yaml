apiVersion: v1
kind: Service
metadata:
  name: apache
  namespace: apache
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: apache
  type: ClusterIP
